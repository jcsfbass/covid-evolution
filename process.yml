apps:
  - name: 'covid-evolution'
    script: ./app/bin/www
    instances: 0
    exec_mode: cluster
    watch: true
    time: true
    no-autorestart: true
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
